<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TravelRecord.AddCompanyData">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Edit car"
                     Icon="edit.png"
                     Order="Primary"/>
        <ToolbarItem Text="Settings"
                     Order="Secondary"/>
    </ContentPage.ToolbarItems>


    <ContentPage.Content>
        <StackLayout>
            <Entry x:Name="CompanyName" Placeholder="Cég neve" VerticalOptions="Center"/>
            <Entry x:Name="CompanyAddress" Placeholder="Cég címe" VerticalOptions="Center"/>
            <Entry x:Name="CompanyVAT" Placeholder="Adószám" VerticalOptions="Center"/>
            <Button Text="Rendben" VerticalOptions="Center" Clicked="Button_AddCompanyDataToDB"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>